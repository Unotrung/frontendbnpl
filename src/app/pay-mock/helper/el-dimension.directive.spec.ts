import { ElDimensionDirective } from './el-dimension.directive';

describe('ElDimensionDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ElDimensionDirective();
    expect(directive).toBeTruthy();
  });
});
