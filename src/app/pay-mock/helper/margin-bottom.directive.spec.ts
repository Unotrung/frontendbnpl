import { MarginBottomDirective } from './margin-bottom.directive';

describe('MarginBottomDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new MarginBottomDirective();
    expect(directive).toBeTruthy();
  });
});
