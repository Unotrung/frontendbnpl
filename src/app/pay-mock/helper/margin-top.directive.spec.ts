import { MarginTopDirective } from './margin-top.directive';

describe('MarginTopDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new MarginTopDirective();
    expect(directive).toBeTruthy();
  });
});
