export interface City {
    id: string;
    code: string;
    name: string;
}

export interface District {
    id: string;
    name: string;
    code: string;
}

export interface Ward {
    id: string;
    name: string;
    code: string;
    prefix: string;
}

export interface Street {
    id: string;
    name: string;
    code: string;
}